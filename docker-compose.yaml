version: '3.8'

services:
  api-go:
    build: ./go-api
    ports:
      - "8081:8080" # Expone Go en el puerto 8081 de tu PC

  api-rust:
    build: ./rust-api
    ports:
      - "8082:8080" # Expone Rust en el puerto 8082 de tu PC